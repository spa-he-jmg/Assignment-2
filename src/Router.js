export function router(path) {
    try {

        
    } catch (error) {
        
    }
    switch (path) {
        case 'game':
        
            break;
        case 'game-over':

            break;
        default:

            break;
    }
}
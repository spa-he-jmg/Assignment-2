/**
 * Mounts component to root
 * @param {HTMLElement} root - Target container for component
 * @param {Object} component - Object with element and handers
 */
function mount(root, {element, handlers}) {

    // Write element to root
    
}